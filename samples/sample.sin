// sample.sin
// A test file for our compiler

def struct point {
	alloc int x;
	alloc int y;
	alloc ptr<int> z;
}

def void main() {
	alloc point p;
	alloc ptr< ptr< int > > x;
	let x = p.z;
	alloc int y: **x;

	return void;
}
