// point_include.sin
// demonstrate declarative SIN

def struct point {  // structs don't need to be declared -- they can be defined in a header
    alloc int x;
    alloc int y;
    alloc int z;
}

decl ptr<point> make_point_2d(decl int x, decl int y);  // functions that aren't 'extern' must be declared
